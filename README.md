# This folder cotains code for backend deployment on Render

## Changes made

### Cleaner and shorter project requirements.txt
### ML skin model and lables moved to lazy loading with get_model_and_labels()
### Added render.yaml to automate deployments and replication of the application stack from Git repository


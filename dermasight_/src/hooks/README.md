# Hooks

Purpose:
  This folder contains custom react hooks for use in the components

Use:
  useApi - reusable stateful logic for api access
    - useAuth - api call for authentication
    - useChat - api call to get chatbot response
    - useUpload - api call to upload image and questionnaire data
    - useUserReports - api call to get user reports

Dependencies:
  React